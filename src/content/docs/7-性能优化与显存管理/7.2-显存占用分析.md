---
title: "7.2 显存占用分析"
description: 显存被谁吃掉了 —— 把权重、激活值、VAE 与缓存各自占多少分清楚，爆显存时才知道先动哪一处。
---

# 7.2 显存占用分析

> **本文承接：**
> [7.1 生成速度 Benchmark](./7.1-生成速度benchmark.md) 给出了量速度的方法；本文量另一条轴 —— 显存。显存常常是硬约束：速度慢只是等得久，显存不够则根本跑不起来。
>
> **本文引出：**
> 量清楚占用之后才有处置的可能：[7.5 显存优化](./7.5-显存优化.md) 专门讲怎么把它压下去；而「算得更快」的那一路从 [7.3 推理加速](./7.3-推理加速.md) 开始。

> **相关官方文档：** [ComfyUI 官方文档](https://docs.comfy.org/) | [中文文档](https://docs.comfy.org/zh/)

## 概述

### 什么是显存？

显存（Video Memory，VRAM）是显卡上的专用内存，用于存储 GPU 处理过程中需要的数据。与普通内存（RAM）不同，显存具有更高的带宽，专门为图形处理和深度学习计算优化。

### 为什么显存很重要？

| 重要性 | 说明 |
|-------|------|
| **模型加载** | 所有模型权重都需要加载到显存 |
| **计算过程** | 中间计算结果占用大量显存 |
| **分辨率限制** | 分辨率越高，显存需求越大 |
| **批量处理** | 批量大小直接影响显存占用 |

### 显存不足会发生什么？

当显存不足时，会出现 **OOM（Out of Memory）** 错误：
- 生成中断，无法完成
- 程序崩溃
- 系统变卡

### 本章你将学到什么？

1. 各组件的显存占用情况
2. 如何监控显存使用
3. 如何估算工作流的显存需求
4. 显存不足时的解决方案

---

## 显存占用基础

### 显存用途分类

| 类型 | 说明 | 占用特点 | 管理方式 |
|-----|------|---------|---------|
| **模型权重** | 神经网络参数 | 固定，加载后不变 | 卸载到内存 |
| **激活值** | 中间计算结果 | 动态，随分辨率变化 | 难以优化 |
| **Latent** | 潜空间数据 | 与分辨率相关 | 占用较小 |
| **临时缓存** | 计算中间值 | 动态分配释放 | 自动管理 |

### 基础显存占用

ComfyUI 启动后的基础显存占用约 **500MB - 1GB**，包括：

| 组件 | 占用 | 说明 |
|-----|------|------|
| PyTorch 运行时 | ~300MB | 深度学习框架 |
| CUDA 上下文 | ~200MB | GPU 计算环境 |
| 基础库加载 | ~100MB | 依赖库 |
| ComfyUI 核心 | ~100MB | 工作流引擎 |

---

## 模型显存占用详解

### Checkpoint 模型

Checkpoint 是占用显存最大的组件：

| 模型类型 | 参数量 | FP16 显存 | FP32 显存 | 推荐显卡 |
|---------|-------|----------|----------|---------|
| SD 1.5 | 0.98B | ~2GB | ~4GB | 6GB+ |
| SD 2.1 | 0.87B | ~1.8GB | ~3.6GB | 6GB+ |
| SDXL | 2.6B | ~5GB | ~10GB | 10GB+ |
| SD3 Medium | 2B | ~4GB | ~8GB | 8GB+ |
| Flux.1 Dev | 12B | ~24GB | ~48GB | 24GB+ |
| Flux.1 Schnell | 12B | ~24GB | ~48GB | 24GB+ |

### VAE 模型

VAE 负责图像编解码，占用相对较小：

| VAE 类型 | 显存占用 | 说明 |
|---------|---------|------|
| SD 1.5 VAE | ~50MB | 标准 VAE |
| SDXL VAE | ~100MB | 更大模型 |
| FLUX VAE | ~200MB | 最新模型 |
| 自定义 VAE | 50-200MB | 如 vae-ft-mse |

### CLIP 模型

CLIP 负责文本编码：

| CLIP 类型 | 显存占用 | 使用场景 |
|----------|---------|---------|
| CLIP ViT-L/14 (SD 1.5) | ~350MB | SD 1.5 系列 |
| CLIP ViT-G/14 (SDXL) | ~700MB | SDXL 系列 |
| OpenCLIP ViT-bigG/14 | ~1.4GB | SDXL 双编码器 |
| T5-XXL (Flux) | ~5GB | Flux 系列 |

### ControlNet 模型

| ControlNet 类型 | 显存占用 | 说明 |
|----------------|---------|------|
| SD 1.5 ControlNet | ~700MB | 单个 ControlNet |
| SDXL ControlNet | ~1.5GB | 单个 ControlNet |
| 多 ControlNet | 累加 | 每个 ControlNet 独立占用 |

**示例：**
- 1 个 SD 1.5 ControlNet：+700MB
- 2 个 SD 1.5 ControlNet：+1.4GB
- 3 个 SD 1.5 ControlNet：+2.1GB

### LoRA 模型

| LoRA 类型 | 显存占用 | 说明 |
|----------|---------|------|
| 标准 LoRA (rank=128) | ~50-100MB | 常用配置 |
| LoRA (rank=64) | ~30-50MB | 中等配置 |
| LoRA (rank=32) | ~20-30MB | 轻量配置 |
| LyCORIS | ~30-80MB | 其他微调方式 |

---

## Latent 显存占用

### 什么是 Latent？

Latent 是图像在潜空间的表示，是扩散模型的核心数据结构。分辨率越高，Latent 越大。

### 不同分辨率的 Latent 占用

| 分辨率 | Latent 尺寸 | 单张显存 | 计算公式 |
|-------|------------|---------|---------|
| 512x512 | 64x64 | ~50KB | 64×64×4×4字节 |
| 768x768 | 96x96 | ~110KB | 96×96×4×4字节 |
| 1024x1024 | 128x128 | ~200KB | 128×128×4×4字节 |
| 1536x1536 | 192x192 | ~450KB | 192×192×4×4字节 |
| 2048x2048 | 256x256 | ~800KB | 256×256×4×4字节 |

### 批量 Latent 占用

| 批量大小 | 512x512 | 1024x1024 |
|---------|---------|-----------|
| 1 | ~50KB | ~200KB |
| 4 | ~200KB | ~800KB |
| 8 | ~400KB | ~1.6MB |
| 16 | ~800KB | ~3.2MB |

**注意：** Latent 本身占用很小，但计算过程中的激活值占用较大。

---

## 激活值显存占用

### 什么是激活值？

激活值是神经网络在计算过程中产生的中间结果。这是显存占用的主要来源，也是最难优化的部分。

### 采样过程显存

| 分辨率 | 估算激活值显存 | 说明 |
|-------|--------------|------|
| 512x512 | ~1-2GB | SD 1.5 标准分辨率 |
| 768x768 | ~2-4GB | 中等分辨率 |
| 1024x1024 | ~4-6GB | SDXL 标准分辨率 |
| 1536x1536 | ~8-12GB | 高分辨率 |
| 2048x2048 | ~12-18GB | 超高分辨率 |

### 影响因素

| 因素 | 影响 | 说明 |
|-----|------|------|
| 分辨率 | 🔴 平方级增长 | 分辨率翻倍，显存约 4 倍 |
| 批量大小 | 🔴 线性增长 | 批量翻倍，显存翻倍 |
| 采样步数 | 🟡 轻微影响 | 步数增加，显存略增 |
| 采样器类型 | 🟡 轻微影响 | 不同采样器略有差异 |

---

## 完整工作流显存估算

### SD 1.5 文生图（512x512）

| 组件 | 显存占用 | 说明 |
|-----|---------|------|
| 基础占用 | ~1GB | 框架和运行时 |
| Checkpoint | ~2GB | FP16 精度 |
| CLIP | ~350MB | 文本编码器 |
| VAE | ~50MB | 图像编解码 |
| 激活值 | ~1.5GB | 计算中间值 |
| **总计** | **~5GB** | **最低需求** |

### SDXL 文生图（1024x1024）

| 组件 | 显存占用 | 说明 |
|-----|---------|------|
| 基础占用 | ~1GB | 框架和运行时 |
| Checkpoint | ~5GB | FP16 精度 |
| CLIP (双编码器) | ~1GB | OpenCLIP + CLIP |
| VAE | ~100MB | 图像编解码 |
| 激活值 | ~5GB | 计算中间值 |
| **总计** | **~12GB** | **最低需求** |

### SD 1.5 + ControlNet（512x512）

| 组件 | 显存占用 | 说明 |
|-----|---------|------|
| 基础工作流 | ~5GB | 标准 SD 1.5 |
| ControlNet | ~700MB | 单个 ControlNet |
| 预处理 | ~200MB | 边缘检测等 |
| **总计** | **~6GB** | **最低需求** |

### SD 1.5 + 2个 ControlNet

| 组件 | 显存占用 | 说明 |
|-----|---------|------|
| 基础工作流 | ~5GB | 标准 SD 1.5 |
| ControlNet x2 | ~1.4GB | 两个 ControlNet |
| 预处理 | ~400MB | 两种预处理 |
| **总计** | **~7GB** | **最低需求** |

---

## 显存监控方法（详细操作步骤）

### 方法一：Windows 任务管理器

#### 操作步骤

1. **打开任务管理器**
   - 按 `Ctrl + Shift + Esc` 快捷键
   - 或右键点击任务栏，选择"任务管理器"

2. **切换到"性能"选项卡**
   - 点击顶部的"性能"标签

3. **选择 GPU**
   - 在左侧列表中找到"GPU"
   - 如果有多个 GPU，会显示 GPU 0、GPU 1 等

4. **查看显存信息**
   - **专用 GPU 内存**：当前显存使用情况
   - **共享 GPU 内存**：使用系统内存作为显存
   - **GPU 内存**：总显存大小

5. **实时监控**
   - 保持任务管理器窗口打开
   - 运行 ComfyUI 时观察显存变化

#### 界面说明

<svg viewBox="0 0 700 157.8" role="img" aria-label="Windows 任务管理器「性能 → GPU」面板示意：标题显示 GPU 0 为 NVIDIA RTX 3080，下方逐行列出利用率 45%、专用 GPU 内存 6.2/10.0 GB、共享 GPU 内存 0.1/16.0 GB、GPU 内存 6.3/26.0 GB —— 看显存占用要读「专用 GPU 内存」这一行" style="width:100%;height:auto;display:block;margin:1.5rem 0">
  <g fill="currentColor" stroke="currentColor" fill-opacity="0.04" stroke-opacity="0.35" stroke-width="1.2">
    <rect x="8" y="8" width="684" height="141.8" rx="8"/>
  </g>
  <g fill="currentColor" stroke="none" fill-opacity="0.3">
    <rect x="8" y="46" width="684" height="1" rx="0"/>
  </g>
  <g fill="currentColor" font-size="11" font-weight="600">
    <text x="26" y="34" font-size="11">GPU 0: NVIDIA RTX 3080</text>
  </g>
  <g fill="currentColor" fill-opacity="0.85" font-size="10">
    <text x="26" y="69" font-size="10">利用率</text>
    <text x="26" y="89" font-size="10">专用 GPU 内存</text>
    <text x="26" y="109" font-size="10">共享 GPU 内存</text>
    <text x="26" y="129" font-size="10">GPU 内存</text>
  </g>
  <g fill="currentColor" fill-opacity="0.9" font-size="10">
    <text x="674" y="69" font-size="10" text-anchor="end">45%</text>
    <text x="674" y="89" font-size="10" text-anchor="end">6.2/10.0 GB</text>
    <text x="674" y="109" font-size="10" text-anchor="end">0.1/16.0 GB</text>
    <text x="674" y="129" font-size="10" text-anchor="end">6.3/26.0 GB</text>
  </g>
</svg>

### 方法二：NVIDIA-SMI 命令行工具

nvidia-smi 是 NVIDIA 官方提供的 GPU 监控工具，功能强大。

#### 基本使用

1. **打开命令提示符或 PowerShell**
   - 按 `Win + R`，输入 `cmd` 或 `powershell`

2. **运行 nvidia-smi**
   ```bash
   nvidia-smi
   ```

3. **查看输出结果**
   ```text
   +-----------------------------------------------------------------------------+
   | NVIDIA-SMI 535.154.05   Driver Version: 535.154.05   CUDA Version: 12.2     |
   |-------------------------------+----------------------+----------------------+
   | GPU  Name        Persistence-M| Bus-Id        Disp.A | Volatile Uncorr. ECC |
   | Fan  Temp  Perf  Pwr:Usage/Cap|         Memory-Usage | GPU-Util  Compute M. |
   |                               |                      |               MIG M. |
   |===============================+======================+======================|
   |   0  NVIDIA GeForce ...  Off  | 00000000:01:00.0  On |                  N/A |
   | 30%   45C    P8    15W / 320W |   6234MiB / 10240MiB |     45%      Default |
   +-------------------------------+----------------------+----------------------+
   ```

#### 输出解读

| 字段 | 含义 | 示例 |
|-----|------|------|
| GPU Name | GPU 名称 | NVIDIA GeForce RTX 3080 |
| Fan | 风扇转速 | 30% |
| Temp | 温度 | 45C |
| Pwr | 功耗 | 15W / 320W |
| Memory-Usage | 显存使用 | 6234MiB / 10240MiB |
| GPU-Util | GPU 利用率 | 45% |

#### 实时监控命令

**Windows PowerShell：**
```powershell
while ($true) { cls; nvidia-smi; Start-Sleep -Seconds 1 }
```

**Linux/macOS：**
```bash
watch -n 1 nvidia-smi
```

**按 `Ctrl + C` 退出实时监控。**

#### 详细信息查询

```bash
nvidia-smi --query-gpu=name,memory.total,memory.used,memory.free --format=csv
```

输出示例：
```text
name, memory.total [MiB], memory.used [MiB], memory.free [MiB]
NVIDIA GeForce RTX 3080, 10240 MiB, 6234 MiB, 4006 MiB
```

### 方法三：Python 代码监控

#### 基础监控脚本

```python
import torch

def print_gpu_memory():
    if not torch.cuda.is_available():
        print("CUDA 不可用")
        return
    
    allocated = torch.cuda.memory_allocated(0) / 1024**3
    reserved = torch.cuda.memory_reserved(0) / 1024**3
    total = torch.cuda.get_device_properties(0).total_memory / 1024**3
    
    print(f"GPU: {torch.cuda.get_device_name(0)}")
    print(f"总显存: {total:.2f} GB")
    print(f"已分配: {allocated:.2f} GB")
    print(f"已保留: {reserved:.2f} GB")
    print(f"可用: {total - reserved:.2f} GB")

print_gpu_memory()
```

#### 实时监控脚本

```python
import torch
import time

def monitor_gpu(interval=1, duration=60):
    """
    实时监控 GPU 显存
    
    参数：
    - interval: 刷新间隔（秒）
    - duration: 监控时长（秒）
    """
    if not torch.cuda.is_available():
        print("CUDA 不可用")
        return
    
    print("开始监控 GPU 显存...")
    print("-" * 60)
    print(f"{'时间':<10} {'已分配(GB)':<15} {'已保留(GB)':<15} {'利用率':<10}")
    print("-" * 60)
    
    start_time = time.time()
    while time.time() - start_time < duration:
        allocated = torch.cuda.memory_allocated(0) / 1024**3
        reserved = torch.cuda.memory_reserved(0) / 1024**3
        
        elapsed = time.time() - start_time
        print(f"{elapsed:<10.1f} {allocated:<15.2f} {reserved:<15.2f}")
        
        time.sleep(interval)

monitor_gpu(interval=0.5, duration=30)
```

#### 在 ComfyUI 中使用

```python
import torch

class MemoryMonitor:
    def __init__(self):
        self.peak_memory = 0
    
    def update_peak(self):
        current = torch.cuda.memory_allocated(0) / 1024**3
        if current > self.peak_memory:
            self.peak_memory = current
        return current
    
    def get_report(self):
        total = torch.cuda.get_device_properties(0).total_memory / 1024**3
        return {
            "total": f"{total:.2f} GB",
            "peak": f"{self.peak_memory:.2f} GB",
            "current": f"{torch.cuda.memory_allocated(0) / 1024**3:.2f} GB"
        }

monitor = MemoryMonitor()
```

### 方法四：使用 GPU-Z 工具

GPU-Z 是一款免费的 GPU 信息工具，提供详细的监控功能。

#### 下载和安装

1. 访问官网：https://www.techpowerup.com/gpuz/
2. 下载最新版本
3. 安装并运行

#### 功能特点

- 实时显存监控
- 温度、频率监控
- 历史曲线图
- 负载记录

---

## 显存不足的表现与解决

### 常见错误信息

#### 错误类型一：CUDA OOM

```text
CUDA out of memory. Tried to allocate 2.00 GiB
```

**含义：** 尝试分配 2GB 显存失败，显存不足。

#### 错误类型二：Runtime Error

```text
RuntimeError: CUDA error: out of memory
```

**含义：** CUDA 运行时错误，显存耗尽。

#### 错误类型三：Allocation Failed

```text
RuntimeError: CUDA driver error: out of memory
```

**含义：** 驱动层面显存不足。

### 处置方向不在本文

本文的任务是**把账量清楚**：占用由哪几笔构成、峰值出现在哪、怎么监控。至于怎么把它压下去 —— 降精度、减少同时驻留、压峰值，以及出现 OOM 时按优先级逐个尝试的七步顺序 —— 统一在 [7.5 显存优化](7.5-显存优化.md) 里给出。

动手之前先分清是哪一笔账占的，这两类现象的处置方向完全不同：

| 现象 | 主要占用 | 方向 |
|---|---|---|
| 换大模型后立刻 OOM | 权重 | 降精度（BF16/FP8），或分块加载 |
| 换小模型仍 OOM，且与分辨率同步恶化 | 激活值 | 降分辨率、减批量、少挂 ControlNet |
| 连续出图几十张后才 OOM | 缓存与碎片 | 重启 ComfyUI，或留出 `--reserve-vram` |

---
## 按显存选配置

各显存档位对应的显存模式、精度与可跑模型，统一列在 [7.5 显存优化](7.5-显存优化.md)。

---
## 常见问题 FAQ

### Q1: 为什么我的显存占用比预期高？

**可能原因：**

| 原因 | 检查方法 | 解决方案 |
|-----|---------|---------|
| 后台程序占用 | 任务管理器检查 | 关闭其他 GPU 程序 |
| 浏览器 GPU 加速 | 检查浏览器设置 | 关闭硬件加速 |
| 模型未卸载 | 查看模型缓存 | 切换模型时清理缓存 |
| 显存碎片 | 长时间运行后 | 重启 ComfyUI |

### Q2: 如何判断需要多少显存？

**估算方法：**

1. 基础占用：~1GB
2. 模型占用：查看本章表格
3. 激活值：分辨率对应值
4. 预留空间：+1-2GB

**示例计算（SDXL 1024x1024）：**
```text
基础: 1GB
模型: 5GB
CLIP: 1GB
激活值: 5GB
预留: 2GB
总计: 14GB（建议 16GB 显卡）
```

### Q3: 共享 GPU 内存是什么？

**说明：**
- 当显存不足时，系统会使用普通内存
- 速度比显存慢很多
- 出现共享内存使用时，说明显存紧张

### Q4: 如何清理显存？

**方法一：重启 ComfyUI**
```bash
# 关闭 ComfyUI
Ctrl + C

# 重新启动
python main.py
```

**方法二：Python 代码清理**
```python
import torch
import gc

gc.collect()
torch.cuda.empty_cache()
```

### Q5: 多显卡如何分配显存？

**方法一：指定 GPU**
```bash
CUDA_VISIBLE_DEVICES=0 python main.py
```

**方法二：多实例运行**
```bash
# 终端 1
CUDA_VISIBLE_DEVICES=0 python main.py --port 8188

# 终端 2
CUDA_VISIBLE_DEVICES=1 python main.py --port 8189
```

---

## 时效性说明

| 项目 | 说明 |
|-----|------|
| 显存数据 | 基于特定版本测试，可能变化 |
| 新模型 | 新模型可能有不同显存需求 |
| 优化技术 | 新优化可能降低显存占用 |
| 驱动版本 | 驱动更新可能影响显存管理 |

---

## 相关章节

- **上一章**：[7.1 生成速度Benchmark](7.1-生成速度benchmark.md)
- **下一章**：[7.3 推理加速](7.3-推理加速.md)

## 参考资源

- [ComfyUI GitHub](https://github.com/Comfy-Org/ComfyUI)
- [PyTorch 显存管理](https://pytorch.org/docs/stable/notes/cuda.html)
- [NVIDIA 显存优化指南](https://docs.nvidia.com/deeplearning/performance/)

---

**对应练习：** [案例 4.1 性能优化实战](../实战案例/Level4-专家级/案例4.1-性能优化实战.md)｜[案例 4.2 低显存优化实战](../实战案例/Level4-专家级/案例4.2-低显存优化实战.md)
