# 依赖
node_modules/

# 构建产物
dist/
.astro/

# 构建缓存
.vite/

# 环境变量
.env
.env.production
.env.local

# 编辑器与系统
.DS_Store
Thumbs.db
*.swp

# 日志
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# PDF 产物（由 starlight-to-pdf 生成，不进版本库）
*.pdf

# 本地工作区（不属于文档内容，不应被克隆者看到）
# .agents/   —— 改造计划、学习原则、链接审计、构建日志、临时脚本
# .workbuddy/ —— 会话记忆
.agents/
.workbuddy/

# 临时探针文件
_tmp_*
_*.log
