# 案例 1.1: 第一个文生图工作流

> **难度等级：** Level 1 - 入门级  
> **学习目标：** 掌握 ComfyUI 基础操作，完成第一张 AI 图像生成  
> **预计时间：** 30 分钟  
> **官方参考：** [ComfyUI GitHub](https://github.com/comfyanonymous/ComfyUI)
> **相关官方文档：** [ComfyUI 官方文档](https://docs.comfy.org/) | [中文文档](https://docs.comfy.org/zh/)

---

## 📋 案例概述

### 🎯 案例目标

本案例将带你从零开始，搭建一个完整的文生图工作流，生成你的第一张 AI 图像。通过本案例，你将掌握 ComfyUI 的核心操作流程。

### 📚 学习要点

| 序号 | 学习要点 | 重要程度 |
|-----|---------|---------|
| 1 | ComfyUI 界面基本操作 | ⭐⭐⭐⭐⭐ |
| 2 | 工作流节点的基本概念 | ⭐⭐⭐⭐⭐ |
| 3 | 模型加载与选择 | ⭐⭐⭐⭐⭐ |
| 4 | 正向/负向提示词编写 | ⭐⭐⭐⭐ |
| 5 | KSampler（K采样器） 采样器参数配置 | ⭐⭐⭐⭐ |
| 6 | 图像生成与保存 | ⭐⭐⭐⭐ |

### ⏱️ 预计时长

| 阶段 | 时间 | 说明 |
|-----|-----|------|
| 环境准备 | 5-10分钟 | 确认模型已下载并放置正确 |
| 工作流搭建 | 10-15分钟 | 加载默认工作流并配置参数 |
| 测试生成 | 5-10分钟 | 生成图像并调整参数 |

---

## 🔧 前置条件

### ✅ 必备条件检查清单

- [ ] 已安装 ComfyUI（GitHub 版 v0.20.0+ 或 桌面端 v2.0.0+）
- [ ] 已下载至少一个 SD 1.5 模型
- [ ] 显存 ≥ 4GB（推荐 6GB 以上）
- [ ] 磁盘空间 ≥ 10GB（用于存放模型和输出）

### 💻 硬件要求

| 配置项 | 最低要求 | 推荐配置 | 说明 |
|-------|---------|---------|------|
| 显存 | 4GB | 8GB+ | 显存越大，可生成图像越大 |
| 内存 | 8GB | 16GB+ | 影响模型加载速度 |
| 磁盘 | 20GB | 100GB+ | 模型文件较大 |
| GPU | NVIDIA GTX 1060 | RTX 3060+ | NVIDIA 显卡兼容性最好 |

### 📌 版本选择指南

| 版本 | 适合人群 | 优点 | 缺点 |
|-----|---------|------|------|
| **GitHub 版** | 开发者、高级用户 | 功能完整、可定制性强 | 需要手动配置环境 |
| **桌面端** | 新手、普通用户 | 一键安装、界面友好 | 部分高级功能受限 |

> 💡 **新手建议：** 如果你是第一次接触 ComfyUI，建议使用桌面端，可以快速上手。

---

## 📦 所需资源

| 资源类型 | 名称 | 文件大小 | 直接下载链接 | 放置完整路径 |
|---------|------|---------|-------------|-------------|
| Checkpoint | Stable Diffusion v1.5 | 4.2GB | [huggingface.co](https://huggingface.co/runwayml/stable-diffusion-v1-5/resolve/main/v1-5-pruned-emaonly.safetensors) | **放置路径**: `E:\Dev\DevEnvironment\ComfyUI_SDK\models\checkpoints\v1-5-pruned-emaonly.safetensors` |
| VAE | sd-vae-ft-mse | 337MB | [huggingface.co](https://huggingface.co/stabilityai/sd-vae-ft-mse-original/resolve/main/vae-ft-mse-840000-ema-pruned.safetensors) | **放置路径**: `E:\Dev\DevEnvironment\ComfyUI_SDK\models\vae\vae-ft-mse-840000-ema-pruned.safetensors` |

> ⚠️ **重要提示：** 以下所有路径基于你的安装路径 `E:\Dev\DevEnvironment\ComfyUI_SDK`，如果你的路径不同，请相应修改！

### 🔍 如何查找你的 ComfyUI 安装路径？

**方法1：查看 ComfyUI 设置**
1. 打开 ComfyUI
2. 点击顶部菜单 `Settings` → `Directories`
3. 查看 `Models` 路径

**方法2：搜索模型文件**
1. 打开文件资源管理器
2. 搜索 `v1-5-pruned-emaonly.safetensors`
3. 找到后，上一级目录就是 `models\checkpoints`

### 📥 一键下载脚本

> ⚠️ **使用前必读：** 请将脚本中的 `E:\Dev\DevEnvironment\ComfyUI` 替换为你实际的安装路径！

#### GitHub 版

**Windows 用户（PowerShell）：**
```powershell
# 你的 ComfyUI 安装路径
$ComfyUI路径 = "E:\Dev\DevEnvironment\ComfyUI_SDK"

# 创建模型目录
mkdir -p "$ComfyUI路径\models\checkpoints"
mkdir -p "$ComfyUI路径\models\vae"

# 下载 SD 1.5 模型
Invoke-WebRequest -Uri "https://huggingface.co/runwayml/stable-diffusion-v1-5/resolve/main/v1-5-pruned-emaonly.safetensors" -OutFile "$ComfyUI路径\models\checkpoints\v1-5-pruned-emaonly.safetensors"

# 下载 VAE
Invoke-WebRequest -Uri "https://huggingface.co/stabilityai/sd-vae-ft-mse-original/resolve/main/vae-ft-mse-840000-ema-pruned.safetensors" -OutFile "$ComfyUI路径\models\vae\vae-ft-mse-840000-ema-pruned.safetensors"
```

**Linux/Mac 用户（终端）：**
```bash
# 你的 ComfyUI 安装路径
ComfyUI路径="/path/to/your/ComfyUI_SDK"

mkdir -p "$ComfyUI路径/models/checkpoints"
mkdir -p "$ComfyUI路径/models/vae"

wget -O "$ComfyUI路径/models/checkpoints/v1-5-pruned-emaonly.safetensors" "https://huggingface.co/runwayml/stable-diffusion-v1-5/resolve/main/v1-5-pruned-emaonly.safetensors"

wget -O "$ComfyUI路径/models/vae/vae-ft-mse-840000-ema-pruned.safetensors" "https://huggingface.co/stabilityai/sd-vae-ft-mse-original/resolve/main/vae-ft-mse-840000-ema-pruned.safetensors"
```

#### 桌面端

**Windows 用户（PowerShell）：**
```powershell
# 你的 ComfyUI 安装路径
$ComfyUI路径 = "E:\Dev\DevEnvironment\ComfyUI_SDK"

# 创建模型目录
mkdir -p "$ComfyUI路径\models\checkpoints"
mkdir -p "$ComfyUI路径\models\vae"

# 下载 SD 1.5 模型
Invoke-WebRequest -Uri "https://huggingface.co/runwayml/stable-diffusion-v1-5/resolve/main/v1-5-pruned-emaonly.safetensors" -OutFile "$ComfyUI路径\models\checkpoints\v1-5-pruned-emaonly.safetensors"

# 下载 VAE
Invoke-WebRequest -Uri "https://huggingface.co/stabilityai/sd-vae-ft-mse-original/resolve/main/vae-ft-mse-840000-ema-pruned.safetensors" -OutFile "$ComfyUI路径\models\vae\vae-ft-mse-840000-ema-pruned.safetensors"
```

### 📂 完整目录结构说明

你的 ComfyUI 安装路径结构应该是这样的：

```
📁 E:\Dev\DevEnvironment\ComfyUI_SDK\          ← 你的 ComfyUI 安装目录
├── 📁 models\                                 ← 模型文件放在这里
│   ├── 📁 checkpoints\                        ← 大模型
│   │   └── v1-5-pruned-emaonly.safetensors    ← SD 1.5 模型
│   └── 📁 vae\                               ← VAE 模型
│       └── vae-ft-mse-840000-ema-pruned.safetensors
├── 📁 custom_nodes\                           ← 自定义节点包
├── 📁 output\                                 ← 生成结果输出
└── 📄 main.py                                 ← 启动文件（GitHub版）
```

> 💡 **提示：** 如果是桌面端，结构类似，模型同样放在 `models\` 目录下。

### ✅ 验证模型安装

#### 步骤 1：确认你的模型路径是否正确

**方法 1：查看 ComfyUI 设置（推荐）**
1. 打开 ComfyUI
2. 点击菜单 `Settings` → `Directories`
3. 查看 `Models` 路径
4. 确保你的模型文件放在这个路径下的 `checkpoints/` 子目录

**方法 2：检查启动日志**
1. 启动 ComfyUI
2. 查看控制台输出
3. 找到类似这样的日志：
   ```
   Loaded X models from E:\Dev\DevEnvironment\ComfyUI_SDK\models\checkpoints
   ```
4. 确认模型文件在这个目录下

#### 步骤 2：验证模型是否加载成功

1. 添加 `Load Checkpoint（加载模型）` 节点
2. 点击 `ckpt_name` 下拉框
3. 如果能看到 `v1-5-pruned-emaonly.safetensors`，说明安装成功

#### ❌ 如果模型没加载出来怎么办？

| 问题 | 解决方案 |
|-----|---------|
| 模型文件放错目录 | 确认路径是否与 ComfyUI 设置中的 Models 路径一致 |
| 文件名错误 | 确保文件名是 `.safetensors` 或 `.ckpt` 后缀 |
| ComfyUI 没重启 | 添加新模型后需要重启 ComfyUI |
| 权限问题 | 确保对模型目录有读取权限 |

> 💡 **提示：** 如果你不确定自己的路径，可以搜索电脑上的 `v1-5-pruned-emaonly.safetensors` 文件，找到后确认它的所在目录是否与 ComfyUI 设置中的路径一致。

---

## 🚀 实战步骤

### 步骤 1: 启动 ComfyUI

#### GitHub 版

**Windows 用户：**
```bash
# 进入 ComfyUI 目录
cd ComfyUI

# 运行启动脚本（NVIDIA GPU）
run_nvidia_gpu.bat
```

**Linux/Mac 用户：**
```bash
# 进入 ComfyUI 目录
cd ComfyUI

# 启动 ComfyUI
python main.py --listen 127.0.0.1 --port 8188
```

**启动成功标志：**
```
To see the GUI go to: http://127.0.0.1:8188
```

#### 桌面端

1. 找到安装目录中的 `ComfyUI.exe`
2. 双击启动程序
3. 程序会自动打开浏览器并加载界面

> 💡 **提示：** 桌面端启动后会自动打开浏览器窗口，无需手动输入网址。

### 步骤 2: 加载默认工作流

1. 打开浏览器，访问 `http://127.0.0.1:8188`（桌面端会自动打开）
2. 点击右侧面板的 **Load Default** 按钮
3. 或使用快捷键 `Ctrl + D`

> 📝 **说明：** 默认工作流包含文生图所需的所有基本节点，非常适合新手学习。

### 步骤 3: 选择模型

1. 找到 **加载模型（Load Checkpoint）** 节点（通常在左上角）
2. 点击 `ckpt_name` 下拉框
3. 选择已下载的模型（如 `v1-5-pruned-emaonly.safetensors`）

> ⚠️ **注意：** 如果下拉框为空，说明模型未正确放置，请返回"前置条件"部分检查。

### 步骤 4: 输入提示词

**正向提示词节点（第一个 CLIP Text Encode（CLIP文本编码器））：**

```
a beautiful landscape with mountains and a lake, sunset, golden hour, 
masterpiece, best quality, highly detailed, 8k
```

**负向提示词节点（第二个 CLIP Text Encode）：**

```
low quality, blurry, distorted, ugly, bad anatomy, 
worst quality, low resolution
```

> 💡 **提示：** 正向提示词描述你想要的画面，负向提示词描述你不想要的效果。

### 步骤 5: 设置图像尺寸

在 **空Latent图像（Empty Latent Image）** 节点设置：

| 参数 | 值 | 说明 |
|-----|---|------|
| width | 512 | 宽度（像素） |
| height | 512 | 高度（像素） |
| batch_size | 1 | 批量大小（一次生成几张图） |

> ⚠️ **注意：** SD 1.5 模型推荐使用 512x512 尺寸，过大可能导致图像质量下降。

### 步骤 6: 调整采样参数

在 **KSampler** 节点设置：

| 参数 | 推荐值 | 说明 |
|-----|-------|------|
| seed | 随机 | 或固定值便于复现 |
| steps | 20-25 | 采样步数 |
| cfg | 7.0 | 提示词引导强度 |
| sampler_name | euler_ancestral | 采样器 |
| scheduler | normal | 调度器 |
| denoise | 1.0 | 文生图固定为 1.0 |

### 步骤 7: 生成图像

1. 点击右侧面板的 **Queue Prompt** 按钮
2. 或使用快捷键 `Ctrl + Enter`
3. 等待生成完成（约 5-15 秒）

> 📊 **进度查看：** 右侧面板会显示当前进度，节点上也会显示进度条。

### 步骤 8: 查看和保存结果

- 生成完成后，图像会显示在 **Save Image（保存图像）** 节点
- 右键点击图像可保存
- 图像自动保存到 `ComfyUI/output/` 目录

---

## 📊 参数配置详解

### KSampler 参数说明

| 参数 | 范围 | 推荐值 | 影响 |
|-----|------|-------|------|
| seed | 0-2147483647 | 随机 | 相同 seed + 参数 = 相同结果 |
| steps | 1-100 | 20-30 | 越高越精细，但耗时增加 |
| cfg | 1-20 | 5-12 | 越高越遵循提示词，但可能过饱和 |
| denoise | 0-1 | 1.0 | 文生图固定 1.0，图生图 0.3-0.8 |

#### seed（随机种子）

- **作用：** 控制图像生成的随机性
- **特点：** 相同的 seed 值 + 相同的参数 = 完全相同的图像
- **使用场景：**
  - 想要复现某张图像时，使用相同的 seed
  - 想要探索不同效果时，使用随机 seed

#### steps（采样步数）

- **作用：** 控制图像生成的精细程度
- **影响：**
  - 步数太少（<15）：图像可能模糊、细节不足
  - 步数适中（20-30）：质量与速度的最佳平衡
  - 步数太多（>40）：提升有限，但耗时明显增加

#### cfg（提示词引导强度）

- **作用：** 控制图像与提示词的匹配程度
- **影响：**
  - cfg 太低（<5）：图像可能偏离提示词
  - cfg 适中（7-10）：平衡创意与控制
  - cfg 太高（>15）：图像可能过饱和、不自然

#### denoise（重绘强度）

- **作用：** 控制新生成内容的比例
- **文生图：** 固定为 1.0（完全新生成）
- **图生图：** 0.3-0.8（保留部分原图特征）

### 采样器选择

| 采样器 | 速度 | 质量 | 推荐步数 | 适用场景 |
|-------|------|------|---------|---------|
| euler | ⭐⭐⭐ | ⭐⭐ | 15-20 | 快速预览 |
| euler_ancestral | ⭐⭐⭐ | ⭐⭐⭐ | 20-25 | 通用推荐 |
| dpmpp_2m | ⭐⭐ | ⭐⭐⭐⭐ | 20-30 | 高质量生成 |
| dpmpp_2m_sde | ⭐ | ⭐⭐⭐⭐⭐ | 25-35 | 最高质量 |

> 💡 **新手推荐：** 使用 `euler_ancestral` 采样器，步数设置为 20-25，这是最稳定可靠的组合。

---

## 🎯 预期结果

### 完成本案例后，你应该能够：

| 序号 | 能力 | 验证方法 |
|-----|------|---------|
| 1 | 独立启动 ComfyUI | 成功打开 ComfyUI 界面 |
| 2 | 加载并运行默认工作流 | 点击 Queue Prompt 后开始生成 |
| 3 | 选择正确的模型 | 模型下拉框显示已安装的模型 |
| 4 | 编写基础提示词 | 图像内容与提示词基本匹配 |
| 5 | 调整 KSampler 参数 | 理解各参数的作用 |
| 6 | 成功生成并保存图像 | 在 output 目录找到生成的图像 |

### 预期生成效果

- **图像尺寸：** 512x512 像素
- **图像内容：** 与正向提示词描述相符的风景图
- **图像质量：** 清晰、无明显瑕疵
- **生成时间：** 约 5-15 秒（取决于硬件）

### 成功标志

✅ 图像成功显示在 Save Image 节点  
✅ 图像内容与提示词描述基本一致  
✅ 图像已自动保存到 output 目录  

---

## ❓ 常见问题（FAQ）

### Q1: 模型列表为空，无法选择模型怎么办？

**问题描述：** 在 加载模型（Load Checkpoint） 节点的下拉框中看不到任何模型。

**可能原因：**
1. 模型文件未放置到正确目录
2. 模型文件格式不正确
3. ComfyUI 未重启

**解决方案：**

| 步骤 | 操作 | 说明 |
|-----|------|------|
| 1 | 检查模型文件位置 | 确保在 `models/checkpoints/` 目录下 |
| 2 | 检查文件格式 | 必须是 `.safetensors` 或 `.ckpt` 后缀 |
| 3 | 重启 ComfyUI | 添加新模型后必须重启 |
| 4 | 检查文件完整性 | 确保文件大小正确（SD1.5 约 4GB） |

### Q2: 提示"显存不足"（Out of Memory）怎么办？

**问题描述：** 生成图像时出现 CUDA out of memory 错误。

**可能原因：**
1. 显存太小（<4GB）
2. 图像尺寸设置过大
3. 其他程序占用显存

**解决方案：**

| 方案 | 操作 | 适用情况 |
|-----|------|---------|
| 方案1 | 降低图像尺寸 | 尝试 512x512 或更小 |
| 方案2 | 使用低显存模式启动 | `python main.py --lowvram` |
| 方案3 | 关闭其他程序 | 关闭浏览器、游戏等 |
| 方案4 | 使用 CPU 模式 | `python main.py --cpu`（速度很慢） |

### Q3: 生成的图像全黑或全白怎么办？

**问题描述：** 生成的图像是纯黑色或纯白色，没有任何内容。

**可能原因：**
1. 模型文件损坏
2. VAE 加载失败
3. 提示词格式错误

**解决方案：**

| 步骤 | 操作 | 说明 |
|-----|------|------|
| 1 | 重新下载模型 | 文件可能下载不完整 |
| 2 | 检查模型文件大小 | SD1.5 应该约 4.2GB |
| 3 | 尝试其他模型 | 排除模型本身问题 |
| 4 | 检查提示词 | 确保没有特殊字符或格式错误 |

### Q4: 生成速度很慢怎么办？

**问题描述：** 生成一张图像需要很长时间（超过 1 分钟）。

**可能原因：**
1. 未启用 xFormers 优化
2. 使用 CPU 模式
3. 硬件性能不足

**解决方案：**

| 方案 | 操作 | 效果 |
|-----|------|------|
| 方案1 | 安装 xFormers | 可提升 20-30% 速度 |
| 方案2 | 降低采样步数 | 从 25 降到 20 |
| 方案3 | 使用更快的采样器 | 如 euler |
| 方案4 | 升级硬件 | 更好的 GPU |

### Q5: 如何保存工作流以便下次使用？

**问题描述：** 想保存当前的工作流配置，下次直接加载。

**解决方案：**

| 操作 | 快捷键 | 说明 |
|-----|-------|------|
| 保存工作流 | `Ctrl + S` | 保存为 .json 文件 |
| 加载工作流 | `Ctrl + O` | 打开已保存的工作流 |
| 导出为图片 | 右键菜单 | 导出包含工作流的图片 |

---

## ⚠️ 时效性说明与版本差异

### 📅 项目状态（2026-04-28）

| 项目 | 版本/状态 | 说明 |
|-----|---------|------|
| ComfyUI (GitHub) | v0.21.0 | 活跃开发中，最近 30 天有 15+ 次提交 |
| ComfyUI 桌面端 | v2.0.0 | 与 GitHub 版功能同步，界面更简洁 |
| 采样器 | 内置稳定 | 新版本可能添加更多采样器选项 |
| 官方文档 | [docs.comfy.org](https://docs.comfy.org/) | 持续更新，权威参考来源 |

### 🆚 GitHub 版 vs 桌面端差异

| 操作 | GitHub 版 | 桌面端 |
|-----|----------|--------|
| 启动方式 | `python main.py` | 双击应用程序 |
| 模型目录 | `ComfyUI/models/` | `ComfyUI/models/` |
| 命令行参数 | 完整支持 | 部分支持（通过设置界面） |
| 自定义节点 | 手动安装 | 部分内置，需手动添加额外节点 |
| 更新方式 | `git pull` | 自动更新提示 |

### 📋 版本兼容性

| ComfyUI 版本 | 本案例兼容性 | 说明 |
|-------------|-------------|------|
| v0.20.0+ | ✅ 完全兼容 | 推荐版本 |
| v0.18.0 - v0.19.9 | ✅ 基本兼容 | 界面可能略有不同 |
| v0.17.0 及以下 | ⚠️ 部分功能可能不可用 | 建议升级 |

**版本检查方法：**
```bash
# GitHub 版：查看版本
cd ComfyUI
git log -1 --format="%H %ci"

# 桌面端：点击 Help > About ComfyUI
```

### 🔄 已变更的功能

| 功能 | 旧版本 | 新版本（v0.20.0+） |
|-----|-------|-------------------|
| 采样器列表 | 固定列表 | 动态加载，支持扩展 |
| 模型加载 | 仅支持 safetensors | 支持多种格式 |
| 界面主题 | 单一主题 | 多主题支持 |

---

## 🎓 扩展练习

完成本案例后，尝试以下练习来巩固所学知识：

### 练习 1：探索不同 Seed 值

**目标：** 理解 seed 对图像生成的影响

**步骤：**
1. 将 seed 设置为固定值（如 12345）
2. 生成图像并保存
3. 再次生成（相同 seed），对比结果
4. 改变 seed 值，观察图像变化

**预期收获：** 理解 seed 的作用，学会复现图像

### 练习 2：调整采样步数

**目标：** 找到质量与速度的最佳平衡点

**步骤：**
1. 分别使用 steps = 10, 15, 20, 25, 30 生成图像
2. 对比图像质量和生成时间
3. 记录你认为最佳的步数值

**预期收获：** 理解 steps 参数的作用，找到适合自己的设置

### 练习 3：尝试不同采样器

**目标：** 了解不同采样器的特点

**步骤：**
1. 分别使用 euler, euler_ancestral, dpmpp_2m 采样器
2. 保持其他参数不变
3. 对比生成效果和速度

**预期收获：** 学会选择合适的采样器

### 练习 4：修改图像尺寸

**目标：** 探索不同尺寸的生成效果

**步骤：**
1. 尝试 512x512, 768x512, 512x768 等不同尺寸
2. 观察不同尺寸下的图像质量
3. 记录最佳尺寸设置

**预期收获：** 理解尺寸对生成效果的影响

### 练习 5：提示词实验

**目标：** 学习编写更有效的提示词

**步骤：**
1. 修改正向提示词，尝试不同主题
2. 调整负向提示词，观察效果变化
3. 尝试添加质量标签（如 masterpiece, best quality）

**预期收获：** 掌握基础提示词编写技巧

---

## ✅ 学习检查清单

完成以下检查清单，确保你已经掌握了本案例的核心内容：

- [ ] 成功启动 ComfyUI
- [ ] 加载并运行默认工作流
- [ ] 理解正向/负向提示词的作用
- [ ] 掌握 KSampler 基本参数调整
- [ ] 成功生成并保存第一张图像
- [ ] 理解 seed、steps、cfg 参数的含义
- [ ] 能够独立完成一次完整的文生图流程
